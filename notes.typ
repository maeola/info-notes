#set page(numbering: "1", number-align: center)
#set text(font: "Linux Libertine")
#set math.mat(delim: "[")

#show link: set text(blue)
#show ref: set text(blue)
#show heading.where(level: 1): set text(orange)
#show heading.where(level: 2): set text(purple)

#set heading(numbering: "1.a")

#align(center, text(3em)[
  *Competitive Programming Notes*
])

#align(center, text(1.3em, gray)[
  Only for Personal Use
])

#align(center, text(1.1em, aqua)[
  dorian
])

#pagebreak()

#outline(depth: 2, indent: true)

#pagebreak()

#include "content/dp.typ"
#pagebreak()

#include "content/segtrees.typ"
#pagebreak()

#include "content/maths.typ"
#pagebreak()

#include "content/graph.typ"
#pagebreak()

#include "content/adhoc.typ"
#pagebreak()

#include "content/strings.typ"
#pagebreak()

#include "content/cg.typ"
#pagebreak()

#include "content/game.typ"
#pagebreak()

#include "content/tricks.typ"
#pagebreak()

#include "content/pe.typ"
#pagebreak()

#include "content/setup.typ"
#pagebreak()

#include "content/ps.typ"
